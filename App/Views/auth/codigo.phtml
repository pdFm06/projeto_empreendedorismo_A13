<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
      crossorigin="anonymous"
    />
    <style>
      html,
      body {
        height: 100%;
      }
      body {
        display: flex;
        align-items: center;
        justify-content: center;
        background: #f1f3f5;
        margin: 0;
      }
      .login-card {
        width: 100%;
        max-width: 500px;
        border-radius: 12px;
        box-shadow: 0 6px 24px rgba(18, 38, 63, 0.08);
      }
    </style>
  </head>
  <body>
    <main class="card login-card p-4 bg-white">
      <div class="card-body">
        <form action="/redefinirPassword" method="post">
          <h4 class="card-title mb-3 text-center">Código Verificação</h4>
          <p class="text-center small text-muted">
            Introduza o código de 6 dígitos enviado para o seu e-mail.
          </p>
          <div class="form-floating mb-3">
            <input
              name="token"
              type="text"
              class="form-control"
              id="floatingInput"
              placeholder="123456"
            />
            <label for="floatingInput">Código (6 dígitos)</label>
          </div>
          <?php if (!empty($this->view->mensagem)): ?>
          <h6 style="color: red"><?= $this->view->mensagem ?></h6>
          <?php endif; ?>
          <button type="submit" class="btn btn-primary w-100">Alterar</button>
          <p class="text-center mt-3 small">
            Não recebeu o código?
            <a href="/reenviarToken" class="text-decoration-none">Reenviar</a>
          </p>
        </form>
      </div>
    </main>
  </body>
</html>
