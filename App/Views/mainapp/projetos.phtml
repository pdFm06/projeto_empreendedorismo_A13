<style>
  .page-header h1 {
    font-weight: 800;
    margin-bottom: 0.35rem;
  }

  .subtitle {
    color: #6b7280;
    margin-bottom: 0;
  }

  .project-card {
    background: #fff;
    border-radius: 18px;
    padding: 1.25rem;
    box-shadow: 0 8px 30px rgba(0,0,0,0.06);
    height: 100%;
  }

  .project-status {
    display: inline-block;
    padding: 0.35rem 0.7rem;
    border-radius: 999px;
    font-size: 0.8rem;
    font-weight: 600;
    background: #e9f2ff;
    color: #0d6efd;
  }

  .status-planeado { background: #fff3cd; color: #856404; }
  .status-em_execucao { background: #d1ecf1; color: #0c5460; }
  .status-concluido { background: #d4edda; color: #155724; }
  .status-suspenso { background: #f8d7da; color: #721c24; }

  .project-title {
    font-size: 1.2rem;
    font-weight: 700;
    margin-bottom: 0;
  }

  .project-info {
    display: grid;
    gap: 0.5rem;
  }

  .info-row {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
  }

  .info-label {
    color: #6b7280;
    font-weight: 600;
  }

  .info-value {
    text-align: right;
  }

  .muted {
    color: #6b7280;
  }

  .view-btn {
    text-decoration: none;
    font-weight: 600;
  }

  .team-box {
    border: 1px solid rgba(0,0,0,0.08);
    border-radius: 12px;
    padding: 0.75rem;
    margin-top: 0.5rem;
  }

  footer {
    color: #6b7280;
    font-size: 0.9rem;
    text-align: center;
    margin-top: 2rem;
  }
</style>

<div class="container py-4">
  <div class="page-header">
    <h1>Projetos</h1>
    <p class="subtitle">Acompanhe todos os projetos em desenvolvimento</p>
  </div>

  <div class="mb-4 mt-4">
    <button
      class="btn btn-primary custom"
      data-bs-toggle="modal"
      data-bs-target="#createProjectModal"
    >
      + Novo Projeto
    </button>
  </div>

  <div class="row g-4 mb-5" id="projectsGrid">
    <?php if (!empty($this->view->projetos)) { ?>
      <?php foreach ($this->view->projetos as $projeto) { ?>
        <?php
          $equipasIds = [];
          if (!empty($projeto['equipas'])) {
            foreach ($projeto['equipas'] as $eq) {
              $equipasIds[] = $eq['equipa_id'];
            }
          }

          $recursosIds = [];
          if (!empty($projeto['recursos'])) {
            foreach ($projeto['recursos'] as $recursoProjeto) {
              $recursosIds[] = $recursoProjeto['recurso_id'];
            }
          }
        ?>

        <div class="col-md-6 col-lg-4">
          <div class="project-card">
            <div style="display:flex;justify-content:space-between;align-items:center">
              <div>
                <div class="project-status status-<?= htmlspecialchars($projeto['estado']) ?>">
                  <?= ucfirst(str_replace('_', ' ', htmlspecialchars($projeto['estado']))) ?>
                </div>
                <h3 class="project-title" style="margin-top:8px">
                  <?= htmlspecialchars($projeto['nome']) ?>
                </h3>
              </div>

              <div class="text-end">
                <button
                  class="btn btn-sm btn-outline-secondary me-2 edit-project-btn"
                  data-bs-toggle="modal"
                  data-bs-target="#editProjectModal"
                  data-id="<?= $projeto['id'] ?>"
                  data-nome="<?= htmlspecialchars($projeto['nome']) ?>"
                  data-gestor-id="<?= htmlspecialchars($projeto['gestor_id']) ?>"
                  data-gestor-nome="<?= htmlspecialchars($projeto['gestor_nome']) ?>"
                  data-prazo="<?= htmlspecialchars($projeto['data_fim_prevista'] ?? '') ?>"
                  data-inicio="<?= htmlspecialchars($projeto['data_inicio'] ?? '') ?>"
                  data-localizacao="<?= htmlspecialchars($projeto['localizacao'] ?? '') ?>"
                  data-descricao="<?= htmlspecialchars($projeto['descricao'] ?? '') ?>"
                  data-estado="<?= htmlspecialchars($projeto['estado']) ?>"
                  data-orcamento="<?= htmlspecialchars($projeto['orcamento']) ?>"
                >
                  Editar
                </button>

                <a
                  href="/eliminar_projeto?id=<?= $projeto['id'] ?>"
                  class="btn btn-sm btn-outline-danger"
                  onclick="return confirm('Tem a certeza que deseja eliminar este projeto?')"
                >
                  Eliminar
                </a>
              </div>
            </div>

            <div class="project-info" style="margin-top:12px">
              <div class="info-row">
                <span class="info-label">Responsável:</span>
                <span class="info-value"><?= htmlspecialchars($projeto['gestor_nome']) ?></span>
              </div>

              <div class="info-row">
                <span class="info-label">Prazo:</span>
                <span class="info-value">
                  <?= !empty($projeto['data_fim_prevista']) ? htmlspecialchars($projeto['data_fim_prevista']) : '—' ?>
                </span>
              </div>

              <div class="info-row">
                <span class="info-label">Localização:</span>
                <span class="info-value">
                  <?= !empty($projeto['localizacao']) ? htmlspecialchars($projeto['localizacao']) : '—' ?>
                </span>
              </div>

              <div class="info-row">
                <span class="info-label">Orçamento:</span>
                <span class="info-value">
                  <?= number_format((float)$projeto['orcamento'], 2, ',', '.') ?> €
                </span>
              </div>
            </div>

            <?php if (!empty($projeto['descricao'])) { ?>
              <p class="muted" style="margin:12px 0 0">
                <?= htmlspecialchars($projeto['descricao']) ?>
              </p>
            <?php } ?>

            <div class="mt-3">
              <strong>Equipas associadas</strong>

              <?php if (!empty($projeto['equipas'])) { ?>
                <div class="mt-2">
                  <?php foreach ($projeto['equipas'] as $eq) { ?>
                    <div class="team-box d-flex justify-content-between align-items-center">
                      <div>
                        <div><strong><?= htmlspecialchars($eq['nome']) ?></strong></div>
                        <div class="muted" style="font-size:.92rem;">
                          <?= !empty($eq['especialidade']) ? htmlspecialchars($eq['especialidade']) : 'Sem especialidade definida' ?>
                          <?php if (!empty($eq['lider_nome'])) { ?>
                            — Líder: <?= htmlspecialchars($eq['lider_nome']) ?>
                          <?php } ?>
                        </div>
                      </div>

                      <a
                        href="/remover_equipa_projeto?projeto_id=<?= $projeto['id'] ?>&equipa_id=<?= $eq['equipa_id'] ?>"
                        class="btn btn-sm btn-outline-danger"
                        onclick="return confirm('Remover esta equipa do projeto?')"
                      >
                        Remover
                      </a>
                    </div>
                  <?php } ?>
                </div>
              <?php } else { ?>
                <div class="muted mt-2">Ainda não existem equipas associadas a este projeto.</div>
              <?php } ?>
            </div>

            <div class="mt-3">
              <form method="post" action="/adicionar_equipa_projeto" class="d-flex gap-2">
                <input type="hidden" name="projeto_id" value="<?= $projeto['id'] ?>">

                <select name="equipa_id" class="form-control" required>
                  <option value="">Seleciona uma equipa</option>

                  <?php
                    $haEquipasDisponiveis = false;

                    foreach ($this->view->equipasDisponiveis as $equipaDisponivel) {
                      if (!in_array($equipaDisponivel['id'], $equipasIds)) {
                        $haEquipasDisponiveis = true;
                  ?>
                        <option value="<?= $equipaDisponivel['id'] ?>">
                          <?= htmlspecialchars($equipaDisponivel['nome']) ?>
                          <?php if (!empty($equipaDisponivel['especialidade'])) { ?>
                            — <?= htmlspecialchars($equipaDisponivel['especialidade']) ?>
                          <?php } ?>
                        </option>
                  <?php
                      }
                    }
                  ?>
                </select>

                <button
                  type="submit"
                  class="btn btn-primary"
                  <?= !$haEquipasDisponiveis ? 'disabled' : '' ?>
                >
                  Adicionar
                </button>
              </form>

              <?php if (!$haEquipasDisponiveis) { ?>
                <div class="muted mt-2" style="font-size:.92rem;">
                  Todas as equipas disponíveis já estão associadas a este projeto.
                </div>
              <?php } ?>
            </div>

            <div class="mt-3">
              <strong>Recursos associados</strong>

              <?php if (!empty($projeto['recursos'])) { ?>
                <div class="mt-2">
                  <?php foreach ($projeto['recursos'] as $recursoProjeto) { ?>
                    <div class="team-box d-flex justify-content-between align-items-center">
                      <div>
                        <div><strong><?= htmlspecialchars($recursoProjeto['nome']) ?></strong></div>
                        <div class="muted" style="font-size:.92rem;">
                          <?= htmlspecialchars($recursoProjeto['tipo']) ?>
                          — Afetado: <?= (int)$recursoProjeto['quantidade_afetada'] ?>
                          — Stock atual: <?= (int)$recursoProjeto['quantidade'] ?>
                        </div>
                      </div>

                      <a
                        href="/remover_recurso_projeto?projeto_id=<?= $projeto['id'] ?>&recurso_id=<?= $recursoProjeto['recurso_id'] ?>"
                        class="btn btn-sm btn-outline-danger"
                        onclick="return confirm('Remover este recurso do projeto?')"
                      >
                        Remover
                      </a>
                    </div>
                  <?php } ?>
                </div>
              <?php } else { ?>
                <div class="muted mt-2">Ainda não existem recursos associados a este projeto.</div>
              <?php } ?>
            </div>

            <div class="mt-3">
              <form method="post" action="/adicionar_recurso_projeto" class="d-flex gap-2 flex-wrap">
                <input type="hidden" name="projeto_id" value="<?= $projeto['id'] ?>">

                <select name="recurso_id" class="form-control" required style="min-width: 220px;">
                  <option value="">Seleciona um recurso</option>

                  <?php
                    $haRecursosDisponiveis = false;

                    foreach ($this->view->recursosDisponiveis as $recursoDisponivel) {
                      if (!in_array($recursoDisponivel['id'], $recursosIds)) {
                        $haRecursosDisponiveis = true;
                  ?>
                        <option value="<?= $recursoDisponivel['id'] ?>">
                          <?= htmlspecialchars($recursoDisponivel['nome']) ?>
                          — <?= htmlspecialchars($recursoDisponivel['tipo']) ?>
                          — Stock: <?= (int)$recursoDisponivel['quantidade'] ?>
                        </option>
                  <?php
                      }
                    }
                  ?>
                </select>

                <input
                  type="number"
                  name="quantidade_afetada"
                  class="form-control"
                  min="1"
                  placeholder="Qtd."
                  required
                  style="max-width: 100px;"
                >

                <button
                  type="submit"
                  class="btn btn-primary"
                  <?= !$haRecursosDisponiveis ? 'disabled' : '' ?>
                >
                  Adicionar
                </button>
              </form>

              <?php if (!$haRecursosDisponiveis) { ?>
                <div class="muted mt-2" style="font-size:.92rem;">
                  Todos os recursos disponíveis já estão associados a este projeto.
                </div>
              <?php } ?>
            </div>

            <div class="project-footer" style="margin-top:12px">
              <a href="#" class="view-btn">Ver Detalhes</a>
            </div>
          </div>
        </div>
      <?php } ?>
    <?php } else { ?>
      <div class="col-12">
        <div class="alert alert-info">
          Ainda não existem projetos registados.
        </div>
      </div>
    <?php } ?>
  </div>

  <!-- Modal Criar Projeto -->
  <div
    class="modal fade"
    id="createProjectModal"
    tabindex="-1"
    aria-labelledby="createProjectModalLabel"
    aria-hidden="true"
  >
    <div class="modal-dialog modal-lg">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="createProjectModalLabel">
            Criar Novo Projeto
          </h5>
          <button
            type="button"
            class="btn-close"
            data-bs-dismiss="modal"
            aria-label="Close"
          ></button>
        </div>

        <div class="modal-body">
          <form id="projectForm" method="post" action="/criar_projeto">
            <div class="row">
              <div class="col-md-6">
                <div class="mb-3">
                  <label for="projectName" class="form-label">Nome do Projeto</label>
                  <input
                    type="text"
                    class="form-control"
                    id="projectName"
                    name="nome"
                    required
                  />
                </div>

                <div class="mb-3">
                  <label for="projectManager" class="form-label">Responsável</label>
                  <select id="projectManager" name="gestor_id" class="form-control">
                    <option value="">Selecione responsável</option>
                    <?php foreach ($this->view->gestores as $gestor) { ?>
                      <option value="<?= $gestor['id'] ?>">
                        <?= htmlspecialchars($gestor['email']) ?>
                      </option>
                    <?php } ?>
                  </select>
                </div>

                <div class="mb-3">
                  <label for="projectDeadline" class="form-label">Prazo</label>
                  <input
                    type="date"
                    class="form-control"
                    id="projectDeadline"
                    name="data_fim_prevista"
                    required
                  />
                </div>

                <div class="mb-3">
                  <label for="projectLocation" class="form-label">Localização</label>
                  <input
                    type="text"
                    class="form-control"
                    id="projectLocation"
                    name="localizacao"
                  />
                </div>
              </div>

              <div class="col-md-6">
                <div class="mb-3">
                  <label for="projectDescription" class="form-label">Descrição</label>
                  <textarea
                    id="projectDescription"
                    name="descricao"
                    class="form-control"
                    rows="4"
                  ></textarea>
                </div>

                <div class="mb-3">
                  <label for="projectState" class="form-label">Estado</label>
                  <select id="projectState" name="estado" class="form-control">
                    <option value="planeado">Planeado</option>
                    <option value="em_execucao">Em execução</option>
                    <option value="concluido">Concluído</option>
                    <option value="suspenso">Suspenso</option>
                  </select>
                </div>

                <div class="mb-3">
                  <label for="projectBudget" class="form-label">Orçamento</label>
                  <input
                    type="number"
                    step="0.01"
                    min="0"
                    class="form-control"
                    id="projectBudget"
                    name="orcamento"
                  />
                </div>

                <div class="mb-3">
                  <label for="projectStart" class="form-label">Data de início</label>
                  <input
                    type="date"
                    class="form-control"
                    id="projectStart"
                    name="data_inicio"
                  />
                </div>
              </div>
            </div>
          </form>
        </div>

        <div class="modal-footer">
          <button
            type="button"
            class="btn btn-secondary"
            data-bs-dismiss="modal"
          >
            Cancelar
          </button>
          <button
            type="submit"
            class="btn btn-primary"
            form="projectForm"
          >
            Criar Projeto
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- Modal Editar Projeto -->
  <div
    class="modal fade"
    id="editProjectModal"
    tabindex="-1"
    aria-labelledby="editProjectModalLabel"
    aria-hidden="true"
  >
    <div class="modal-dialog modal-lg">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="editProjectModalLabel">
            Editar Projeto
          </h5>
          <button
            type="button"
            class="btn-close"
            data-bs-dismiss="modal"
            aria-label="Close"
          ></button>
        </div>

        <div class="modal-body">
          <form id="editProjectForm" method="post" action="/editar_projeto">
            <input type="hidden" id="editProjectId" name="id" />

            <div class="row">
              <div class="col-md-6">
                <div class="mb-3">
                  <label for="editProjectName" class="form-label">Nome</label>
                  <input
                    type="text"
                    id="editProjectName"
                    name="nome"
                    class="form-control"
                    required
                  />
                </div>

                <div class="mb-3">
                  <label for="editProjectManager" class="form-label">Responsável</label>
                  <select
                    id="editProjectManager"
                    name="gestor_id"
                    class="form-control"
                    required
                  >
                    <option value="">Selecione responsável</option>
                    <?php foreach ($this->view->gestores as $gestor) { ?>
                      <option value="<?= $gestor['id'] ?>">
                        <?= htmlspecialchars($gestor['email']) ?>
                      </option>
                    <?php } ?>
                  </select>
                </div>

                <div class="mb-3">
                  <label for="editProjectDeadline" class="form-label">Prazo</label>
                  <input
                    type="date"
                    id="editProjectDeadline"
                    name="data_fim_prevista"
                    class="form-control"
                  />
                </div>

                <div class="mb-3">
                  <label for="editProjectStart" class="form-label">Data de início</label>
                  <input
                    type="date"
                    id="editProjectStart"
                    name="data_inicio"
                    class="form-control"
                  />
                </div>
              </div>

              <div class="col-md-6">
                <div class="mb-3">
                  <label for="editProjectLocation" class="form-label">Localização</label>
                  <input
                    type="text"
                    id="editProjectLocation"
                    name="localizacao"
                    class="form-control"
                  />
                </div>

                <div class="mb-3">
                  <label for="editProjectState" class="form-label">Estado</label>
                  <select
                    id="editProjectState"
                    name="estado"
                    class="form-control"
                  >
                    <option value="planeado">Planeado</option>
                    <option value="em_execucao">Em execução</option>
                    <option value="concluido">Concluído</option>
                    <option value="suspenso">Suspenso</option>
                  </select>
                </div>

                <div class="mb-3">
                  <label for="editProjectBudget" class="form-label">Orçamento</label>
                  <input
                    type="number"
                    step="0.01"
                    min="0"
                    id="editProjectBudget"
                    name="orcamento"
                    class="form-control"
                  />
                </div>
              </div>

              <div class="col-12">
                <div class="mb-3">
                  <label for="editProjectDescription" class="form-label">Descrição</label>
                  <textarea
                    id="editProjectDescription"
                    name="descricao"
                    class="form-control"
                    rows="5"
                  ></textarea>
                </div>
              </div>
            </div>
          </form>
        </div>

        <div class="modal-footer">
          <button
            type="button"
            class="btn btn-secondary"
            data-bs-dismiss="modal"
          >
            Cancelar
          </button>

          <a
            href="#"
            id="deleteProjectLink"
            class="btn btn-danger"
            onclick="return confirm('Tem a certeza que deseja eliminar este projeto?')"
          >
            Eliminar
          </a>

          <button
            type="submit"
            class="btn btn-primary"
            form="editProjectForm"
          >
            Guardar Alterações
          </button>
        </div>
      </div>
    </div>
  </div>

  <footer>
    © <span id="year"></span> Projeto Empreendedorismo — ISCTE ·
    Desenvolvido pelo Grupo A13
  </footer>
</div>

<script>
  document.getElementById("year").textContent = new Date().getFullYear();

  document.querySelectorAll(".edit-project-btn").forEach((btn) => {
    btn.addEventListener("click", function () {
      document.getElementById("editProjectId").value = this.dataset.id || "";
      document.getElementById("editProjectName").value = this.dataset.nome || "";
      document.getElementById("editProjectManager").value = this.dataset.gestorId || "";
      document.getElementById("editProjectDeadline").value = this.dataset.prazo || "";
      document.getElementById("editProjectStart").value = this.dataset.inicio || "";
      document.getElementById("editProjectLocation").value = this.dataset.localizacao || "";
      document.getElementById("editProjectDescription").value = this.dataset.descricao || "";
      document.getElementById("editProjectState").value = this.dataset.estado || "planeado";
      document.getElementById("editProjectBudget").value = this.dataset.orcamento || "";

      document.getElementById("deleteProjectLink").href =
        "/eliminar_projeto?id=" + (this.dataset.id || "");
    });
  });
</script>