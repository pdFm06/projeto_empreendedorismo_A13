<?php
use App\Lib\Flash;
$flash = Flash::get();
$tema = $_SESSION['tema'] ?? 'light';
?>
<!DOCTYPE html>
<html lang="pt" data-theme="<?= htmlspecialchars($tema) ?>">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Autenticação</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
      crossorigin="anonymous"
    />

    <style>
      html[data-theme="light"] {
        --bg-main: linear-gradient(135deg, #f8fafc 0%, #eef4ff 100%);
        --card-bg: #ffffff;
        --text-main: #111827;
        --text-muted: #6b7280;
        --shadow-soft: 0 12px 40px rgba(15, 23, 42, 0.08);
      }

      html[data-theme="dark"] {
        --bg-main: linear-gradient(135deg, #0f172a 0%, #111827 100%);
        --card-bg: #111827;
        --text-main: #e5e7eb;
        --text-muted: #9ca3af;
        --shadow-soft: 0 12px 40px rgba(0, 0, 0, 0.35);
      }

      body {
        min-height: 100vh;
        margin: 0;
        background: var(--bg-main);
        font-family: "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 24px;
        color: var(--text-main);
        transition: background .2s ease, color .2s ease;
      }

      .auth-wrapper {
        width: 100%;
        max-width: 520px;
      }

      .brand-box {
        text-align: center;
        margin-bottom: 1.25rem;
      }

      .brand-title {
        font-size: 1.8rem;
        font-weight: 800;
        color: #0d6efd;
        margin-bottom: 0.35rem;
      }

      .brand-subtitle {
        color: var(--text-muted);
        margin-bottom: 0;
      }

      .auth-card {
        background: var(--card-bg);
        border-radius: 20px;
        box-shadow: var(--shadow-soft);
        padding: 1.75rem;
        color: var(--text-main);
      }

      .alert {
        border-radius: 12px;
      }

      .form-control,
      .form-select {
        border-radius: 12px;
        background-color: <?= $tema === 'dark' ? '#1f2937' : '#ffffff' ?>;
        color: var(--text-main);
        border-color: <?= $tema === 'dark' ? 'rgba(255,255,255,0.12)' : '#ced4da' ?>;
      }

      .form-control:focus,
      .form-select:focus {
        background-color: <?= $tema === 'dark' ? '#1f2937' : '#ffffff' ?>;
        color: var(--text-main);
      }

      .form-floating > label {
        color: var(--text-muted);
      }

      .btn {
        border-radius: 12px;
      }

      .auth-footer {
        text-align: center;
        margin-top: 1rem;
        color: var(--text-muted);
        font-size: 0.9rem;
      }

      .auth-footer a {
        text-decoration: none;
      }

      .btn-close {
        filter: <?= $tema === 'dark' ? 'invert(1)' : 'none' ?>;
      }
    </style>
  </head>
  <body>
    <div class="auth-wrapper">
      <div class="brand-box">
        <div class="brand-title">OnBUILD</div>
        <p class="brand-subtitle">Gestão de projetos de construção</p>
      </div>

      <?php if ($flash) { ?>
        <div class="alert alert-<?= htmlspecialchars($flash['tipo']) ?> alert-dismissible fade show mb-3" role="alert">
          <?= htmlspecialchars($flash['mensagem']) ?>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fechar"></button>
        </div>
      <?php } ?>

      <div class="auth-card">
        <?php $this->content() ?>
      </div>

      <div class="auth-footer">
        Projeto Empreendedorismo — ISCTE · Grupo A13
      </div>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
      crossorigin="anonymous"
    ></script>
  </body>
</html>