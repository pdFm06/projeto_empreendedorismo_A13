<?php
use App\Lib\Flash;
$flash = Flash::get();
?>
<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Autenticação</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
      crossorigin="anonymous"
    />

    <style>
      body {
        min-height: 100vh;
        margin: 0;
        background: linear-gradient(135deg, #f8fafc 0%, #eef4ff 100%);
        font-family: "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 24px;
      }

      .auth-wrapper {
        width: 100%;
        max-width: 520px;
      }

      .brand-box {
        text-align: center;
        margin-bottom: 1.25rem;
      }

      .brand-title {
        font-size: 1.8rem;
        font-weight: 800;
        color: #0d6efd;
        margin-bottom: 0.35rem;
      }

      .brand-subtitle {
        color: #6b7280;
        margin-bottom: 0;
      }

      .auth-card {
        background: #fff;
        border-radius: 20px;
        box-shadow: 0 12px 40px rgba(15, 23, 42, 0.08);
        padding: 1.75rem;
      }

      .alert {
        border-radius: 12px;
      }

      .form-control,
      .form-select {
        border-radius: 12px;
      }

      .btn {
        border-radius: 12px;
      }

      .auth-footer {
        text-align: center;
        margin-top: 1rem;
        color: #6b7280;
        font-size: 0.9rem;
      }

      .auth-footer a {
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <div class="auth-wrapper">
      <div class="brand-box">
        <div class="brand-title">OnBUILD</div>
        <p class="brand-subtitle">Gestão de projetos de construção</p>
      </div>

      <?php if ($flash) { ?>
        <div class="alert alert-<?= htmlspecialchars($flash['tipo']) ?> alert-dismissible fade show mb-3" role="alert">
          <?= htmlspecialchars($flash['mensagem']) ?>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fechar"></button>
        </div>
      <?php } ?>

      <div class="auth-card">
        <?php $this->content() ?>
      </div>

      <div class="auth-footer">
        Projeto Empreendedorismo — ISCTE · Grupo A13
      </div>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
      crossorigin="anonymous"
    ></script>
  </body>
</html>